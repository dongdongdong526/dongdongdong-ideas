# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

No project files, README, Cursor rules, Copilot instructions, build configuration, or test configuration were present when this file was created.

## Development commands

No build, lint, test, or run commands are currently defined in this repository.

When project files are added, update this section with the actual commands from the project's package manager, build system, or README.

## Architecture

No application architecture is currently discoverable because the repository is empty.

When source files are added, update this section with the high-level structure and cross-file relationships that are not obvious from filenames alone.
